<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Jibe" parent="Theme.AppCompat.Light.NoActionBar" />

    <!-- Zero-chrome transparent shell for activities that must briefly enter foreground
         (e.g. clipboard reads on Android 10+) without showing any UI. -->
    <style name="Theme.Jibe.Transparent">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>