"""Clipboard sync between Android and Linux (stub for now).

In a future milestone, this will send `clipboard.sync` messages when the
Linux clipboard changes and apply incoming clipboard updates from Android.
"""
